// canvas
const size = 600;
const gridSize = size / 9;
const gridSizeXL = size / 3;

// frame
const lineThickness = size / 100;

// symbols
const symbolSize = gridSize * 0.7;
const symbolAnimationLength = 0.1;
const crossThickness = 0.15;
const circleThickness = 0.3;

// menu
const menuFontSize = 24;
const menuItemSize = 0.9;
const menuAnimationLength = 0.1;

// game logic
const moveTimeInMilliseconds = 250;